1.Explain in your own simple words, what kind of queries would you like to perform on Students, Courses, or Trainers?
    I need to use objects.all().count() to get total counts of each model ,to display it in home page and objects.all() to display details of each model in respective template pages by loop through it.

2.Explain in your own simple words, if you were building this for a real institute, what feature would you add next? 
    If i build this for real institute , then I need to add many other pages like dashboard, checkout, login , signup and add features to each pages like search, filters, pagination and ensure security by implementing authentication and authorizations(user based permissions).